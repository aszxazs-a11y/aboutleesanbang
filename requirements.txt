# ===================================================================
# requirements.txt - Python 패키지 의존성 목록
# ===================================================================
#
# 이 파일은 프로젝트에 필요한 Python 패키지들을 정의합니다.
# pip install -r requirements.txt 명령으로 한 번에 설치할 수 있습니다.
#
# [패키지 설명]
# - Django: 웹 프레임워크
# - gunicorn: WSGI 서버 (배포용)
# - whitenoise: 정적 파일 서빙 (배포용)
# - Pillow: 이미지 처리 (ImageField 사용에 필요)
# ===================================================================

Django>=6.0
gunicorn>=21.0.0
whitenoise>=6.6.0
Pillow>=10.0.0
